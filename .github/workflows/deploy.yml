name: Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Add server to known_hosts
        run: ssh-keyscan 118.31.33.58 >> ~/.ssh/known_hosts

      - name: Deploy to Server
        run: ssh root@118.31.33.58 'cd /var/www/训练1/app && git pull && sudo systemctl restart uwsgi'
